import { Box, Toolbar } from '@material-ui/core'
import React from 'react'
import { isLocalhost } from '../../utils'

export const Vault = () => {

  return (
    <Box>
      <Toolbar />      
    </Box>
  )
}
